// =============================================================================
// ВХОДЯЩИЕ ДАННЫЕ
// =============================================================================
const data = 1



// =============================================================================
// РЕКУРСИВНАЯ ФУНКЦИЯ
// =============================================================================
function func(n) {
  // ПРОСТОЙ СЛУЧАЙ
  // Если:
  // - ...
  // Вернуть:
  // - ...
  if (n === 0) return 0;
  
  // СЛОЖНЫЙ СЛУЧАЙ
  // Если:
  // - ... (все остальные случаи)
  // Сделать:
  // - Вызвать функцию, передав...
  // Вернуть:
  // - Результат вызова функции...
  return func(n - 1);
}



// =============================================================================
// ВЫЗОВ ФУНКЦИИ
// =============================================================================
console.log( func(data) );